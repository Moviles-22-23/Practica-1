pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "nonogramas"
include ':app'
include ':Engine'
include ':DesktopEngine'
include ':Logic'
include ':DesktopLauncher'
include ':AndroidEngine'
